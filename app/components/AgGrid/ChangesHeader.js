import React from 'react';
import SortableIconHeader from './SortableIconHeader';

export default function ChangesHeader(props) {
  return <SortableIconHeader {...props} icon={'keyboard_tab'}></SortableIconHeader>;
}
